import type { ReactNode } from "react";

type PurchaseLayoutProps = {
  children: ReactNode;
};

const PurchaseLayout = ({ children }: PurchaseLayoutProps) => children;

export default PurchaseLayout;
